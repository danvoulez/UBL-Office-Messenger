*** Begin Patch
*** Add File: apps/messenger/frontend/src/theme/ThemeProvider.tsx
+import React, { useEffect, useMemo } from 'react';
+// Try to import the project's Auth hook/context. If the path differs,
+// adjust the import below to your existing AuthContext.
+// Common locations tried: "../context/AuthContext", "../../context/AuthContext"
+// eslint-disable-next-line @typescript-eslint/ban-ts-comment
+// @ts-ignore
+import { useAuth } from '../context/AuthContext';
+
+type Props = { children: React.ReactNode };
+
+/** Heuristic: treat user as admin if any of these hints are present. */
+function isAdminUser(user: any): boolean {
+  if (!user) return false;
+  if (user.is_admin === true) return true;
+  if (user.role && String(user.role).toLowerCase() === 'admin') return true;
+  if (Array.isArray(user.roles) && user.roles.map(String).map(s => s.toLowerCase()).includes('admin')) return true;
+  // Trust levels like "L0".."L5" — consider L4+ as admin
+  if (typeof user.level === 'number' && user.level >= 4) return true;
+  if (user.trust_level) {
+    const m = String(user.trust_level).match(/^L(\d)$/i);
+    if (m && Number(m[1]) >= 4) return true;
+  }
+  return false;
+}
+
+export const ThemeProvider: React.FC<Props> = ({ children }) => {
+  // Fallbacks so this file doesn't crash even if hook path differs.
+  let authUser: any = null;
+  try {
+    const auth = useAuth?.();
+    authUser = auth?.user ?? auth?.currentUser ?? null;
+  } catch (_) {
+    // no-op
+  }
+
+  const admin = useMemo(() => isAdminUser(authUser), [authUser]);
+
+  useEffect(() => {
+    // Allow manual override for development/debug: localStorage.roleTheme = 'blue' | 'orange'
+    const override = (typeof window !== 'undefined' && window.localStorage.getItem('roleTheme')) || null;
+    const target = override === 'blue' || override === 'orange'
+      ? override
+      : (admin ? 'blue' : 'orange');
+
+    const root = document.documentElement;
+    root.classList.remove('theme-blue', 'theme-orange');
+    root.classList.add(target === 'blue' ? 'theme-blue' : 'theme-orange');
+  }, [admin]);
+
+  return <>{children}</>;
+};
+
+export default ThemeProvider;
+
*** End Patch

*** Begin Patch
*** Add File: apps/messenger/frontend/src/theme/index.css
+/* Role-based accent theming via CSS variables.
+   Default (no class) is orange to be safe; explicit classes win. */
+
+:root {
+  --accent-50:  #fff7ed;
+  --accent-100: #ffedd5;
+  --accent-200: #fed7aa;
+  --accent-300: #fdba74;
+  --accent-400: #fb923c;
+  --accent-500: #f97316;
+  --accent-600: #ea580c;
+  --accent-700: #c2410c;
+  --accent-800: #9a3412;
+  --accent-900: #7c2d12;
+  --accent-ring: rgba(234, 88, 12, 0.35);
+}
+
+.theme-orange {
+  --accent-50:  #fff7ed;
+  --accent-100: #ffedd5;
+  --accent-200: #fed7aa;
+  --accent-300: #fdba74;
+  --accent-400: #fb923c;
+  --accent-500: #f97316;
+  --accent-600: #ea580c;
+  --accent-700: #c2410c;
+  --accent-800: #9a3412;
+  --accent-900: #7c2d12;
+  --accent-ring: rgba(234, 88, 12, 0.35);
+}
+
+.theme-blue {
+  --accent-50:  #eff6ff;
+  --accent-100: #dbeafe;
+  --accent-200: #bfdbfe;
+  --accent-300: #93c5fd;
+  --accent-400: #60a5fa;
+  --accent-500: #3b82f6;
+  --accent-600: #2563eb;
+  --accent-700: #1d4ed8;
+  --accent-800: #1e40af;
+  --accent-900: #1e3a8a;
+  --accent-ring: rgba(37, 99, 235, 0.35);
+}
+
+/* Opinionated defaults targeting common UI atoms.
+   If your design system already maps to a "primary" token, you can
+   replace these with your token class names. */
+
+.btn-primary, button[data-variant="primary"] {
+  background-color: var(--accent-600) !important;
+  border-color: var(--accent-600) !important;
+  color: white !important;
+}
+.btn-primary:hover, button[data-variant="primary"]:hover {
+  background-color: var(--accent-700) !important;
+  border-color: var(--accent-700) !important;
+}
+.btn-ghost, button[data-variant="ghost"] {
+  color: var(--accent-700) !important;
+  background-color: transparent !important;
+}
+.badge-primary, [data-badge="primary"] {
+  background-color: var(--accent-100) !important;
+  color: var(--accent-700) !important;
+  border: 1px solid var(--accent-200) !important;
+}
+
+/* Links and focus rings */
+a { color: var(--accent-600); }
+a:hover { color: var(--accent-700); }
+
+*:focus {
+  outline: none;
+  box-shadow: 0 0 0 3px var(--accent-ring);
+}
+
+/* Inputs / sliders / toggles minimal accent */
+input[type="checkbox"]:checked {
+  accent-color: var(--accent-600);
+}
+input[type="radio"]:checked {
+  accent-color: var(--accent-600);
+}
+
*** End Patch

*** Begin Patch
*** Update File: apps/messenger/frontend/src/index.tsx
@@
-import React from 'react'
+import React from 'react'
 import ReactDOM from 'react-dom/client'
 import App from './App.tsx'
 import './index.css'
+import './theme/index.css'
+import { ThemeProvider } from './theme/ThemeProvider'

 ReactDOM.createRoot(document.getElementById('root')!).render(
   <React.StrictMode>
-    <App />
+    <ThemeProvider>
+      <App />
+    </ThemeProvider>
   </React.StrictMode>,
 )
*** End Patch